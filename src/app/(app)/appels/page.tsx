import { PageShell } from "@/components/layout/PageShell";

export default function AppelsPage() {
  return (
    <PageShell
      title="Mes appels"
      subtitle="Historique Call 1 / Call 2 / Call N"
    />
  );
}
